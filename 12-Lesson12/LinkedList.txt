一、链表
1.作用:用于表示由若干个同类型的(复合)元素所构成的具有线性结构的一组数据
2.构成:

二、单链表
1.特点
    >单链表的每一个元素只包含一个指向相邻节点的指针
    >需要一个头指针,指向第一个元素
2.定义:
    struct Node {
        int content;
        struct Node *next;
    }
3.链表程序的主体结构:创建链表,返回头指针
4.对链表的操作:见LinkedList.c

三、


四、


