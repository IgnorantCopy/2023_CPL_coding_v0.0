一、引用类型:给一个变量取一个别名
e.g.
int x = 0;
int &y = x;
y = 1;
printf("x = %d, y = %d\n", x, y);
引用类型和指针类型的区别:
    ①语法上:对引用类型变量的访问与非引用类型相同
    ②语义上:
        >对引用类型变量的访问实际访问的是另一个变量(被引用的变量)
        >效果与通过指针间接访问另一个变量相同
